  [
    constructor(Module, Default, <string(str)>),
    constructor(Modules, List, modules([<first(Module)>,<[next(Modules)]>])),
    constructor(Modules, Body, [<first(Module)>,<[next(Modules)]>]),
    constructor(Modules, Empty, []),
    constructor(Modules2, Empty, []),
    constructor(ModuleList, Modules, list(<modules(Modules)>))
  ]
