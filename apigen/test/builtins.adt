[
  constructor(D, iinteger, int(<integer(int)>)),
  constructor(D, ddouble, double(<number(real)>)),
  constructor(D, trm, term(<trm(term)>)),
  constructor(D, lst, list(<lst(list)>)),
  constructor(D, sstring, str(<string(str)>)),

  constructor(D, all, all(<integer(int)>,<number(real)>,<trm(term)>,<string(str)>)),
  constructor(D, some, some(<integer(int)>,<number(real)>)),
  
  list(IntList, int),
  list(DoubleList, real),
  list(TermList, term),
  list(StringList, str),

  separated-list(SepIntList, int,[<sep(str)>,<pes(str)>]),
  separated-list(SepDoubleList, real,[<sep(str)>,","]),
  separated-list(SepTermList, term,[<sep(str)>,","]),
  separated-list(SepStringList, str,[<sep(str)>,","])
  
]
