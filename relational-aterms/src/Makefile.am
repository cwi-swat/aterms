
lib_LIBRARIES = libATerm-relations.a

include_HEADERS = \
	aterm-relations.h

libATerm_relations_a_SOURCES = \
	aterm-relations.c

AM_CFLAGS = -Wall -pedantic -g -O2

AM_LDFLAGS = \
	-L${ATERM}/lib \
	-L${BALANCED_BINARY_TREES}/lib

INCLUDES = \
        -I${ATERM}/include \
        -I${BALANCED_BINARY_TREES}/include 

